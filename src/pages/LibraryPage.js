import { navigate } from '../services/Router';

const template = `
  <div>
    <span>Library page title</span>
    <a href="/">Home</a>
  </div>
`;

const init = () => {

  return template;
};

export default init;
